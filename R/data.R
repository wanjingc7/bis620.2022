
#' Synthesized UKBiobank Accelerometry Data
#'
#' A toy accelerometry dataset from UKBiobank where:
#'
#' `time` is the time of the measurement;
#' `X` acceleration in the "X" direction;
#' `Y` acceleration in the "Y" direction;
#' `Z` acceleration in the "Z" direction;
#'
#' @name ukb_accel
#' @docType data
#' @keywords data
#'
NULL
#' Synthesized Clinical Trail Data
#'
#' A data set from NCT00460265:
#' `ATRT` is the Actual Treatment;
#' `DTHDY` is the Day of Death;
#' `SUBJID` is the Subject ID of the patients;
#' `AGE` is the Age in Years at Screening;
#' `SEX` is the Sex;
#' `DIAGTYPE` is the Primary Tumor Diagnosis;
#' `HPV` is the HPV Status;
#' `B_ECOG` is the Baseline ECOG Performance Status;
#' `DIAGSTAG` is the Disease Stage at Diagnosis
#' `RACE` is the Race of patients;
#' `AETERM` is the adverse events;
#' `RSRESP` is the Overall Response Status;
#' `RSCONFYN` is the confirmed response

#'
#' @name full_data
#' @docType data
#' @keywords data
NULL
